# This file contains credentials for connecting external messaging channels to your Rasa bot.

rest:
  # Enables REST channel for local testing with curl/Postman or Rasa Webchat
  #

# If you're using Rasa X (optional)
# rasa:
#   url: "http://localhost:5002/api"

# If you want to use Telegram later
# telegram:
#   access_token: "<your-telegram-bot-token>"
#   verify: "<botname>"
#   webhook_url: "https://<your-server>/webhooks/telegram/webhook"

# No password protection (only for local use)
